import 'package:flutter/cupertino.dart';

class TabSearch extends StatelessWidget {
  const TabSearch({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Tab Search'),
    );
  }
}
