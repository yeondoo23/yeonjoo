import 'package:flutter/cupertino.dart';

class TabHeart extends StatelessWidget {
  const TabHeart({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Tab Heart'),
    );
  }
}
