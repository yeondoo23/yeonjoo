import 'package:flutter/cupertino.dart';

class TabNearby extends StatelessWidget {
  const TabNearby({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Tab Nearby'),
    );
  }
}
